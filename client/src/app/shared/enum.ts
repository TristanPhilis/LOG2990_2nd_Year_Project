export enum sideBarToolID {
    move,
    cropping,
    text,
    filling, // More to add
}

export enum drawingToolId {
    pencilService,
    rectangleService,
    ellipseService,
}

export enum MouseButton {
    Left = 0,
    Middle = 1,
    Right = 2,
    Back = 3,
    Forward = 4,
}
