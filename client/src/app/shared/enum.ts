export enum ToolId {
    pencilService,
    rectangleService,
    ellipseService,
    lineService,
}

export enum MouseButton {
    Left = 0,
    Middle = 1,
    Right = 2,
    Back = 3,
    Forward = 4,
}
