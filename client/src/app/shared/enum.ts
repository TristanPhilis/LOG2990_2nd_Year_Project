export enum sidebarToolID {
    move,
    cropping,
    drawing,
    text,
    filling,
    createNew,
    saveCurrent,
    openCarrousel,
    exportCurrent,
    openGuide,
}

export enum drawingToolId {
    pencilService,
    rectangleService,
    ellipseService,
}

export enum MouseButton {
    None = 0,
    Left = 1,
    Middle = 2,
    Right = 3,
    Back = 4,
    Forward = 5,
}
